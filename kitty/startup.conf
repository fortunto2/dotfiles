# Default layout: work | claude (2 columns)

layout splits

# Left - Work terminal
launch --cwd=current --title=work

# Right - Claude Code
launch --location=vsplit --cwd=current --title=claude zsh -ic "claude --dangerously-skip-permissions"
